<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="test">
        <StackPanel Orientation="Horizontal">
            <Ellipse Width="30" Height="30" Margin="10,0,0,-5">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ImageSource}" RenderOptions.BitmapScalingMode="Fant"/>
                </Ellipse.Fill>
            </Ellipse>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding SenderID}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="12"/>
                    <TextBlock Text="{Binding Timestamp}" Foreground="Gray" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="8"/>
                </StackPanel>
                <ListView>
                    <TextBlock Text="{Binding Content}" Foreground="White" Margin="4"/>
                </ListView>
            </StackPanel>

        </StackPanel>
    </DataTemplate>

    <Style x:Key="test2" TargetType="ListView">
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background" Value="LightGray"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
</ResourceDictionary>