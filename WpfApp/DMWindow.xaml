<UserControl x:Class="ChattingWindow.DMWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:WpfApp"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <ListBox x:Name="outputListBox" Height="300" d:ItemsSource="{d:SampleData ItemCount=5}" ></ListBox>
            <TextBox x:Name="inputTextBox" Text="Text" KeyDown="TextBoxKeyDown" Height="30"/>
        </StackPanel>
    </Grid>
    <!--<Grid>
        <TextBlock Text="Home Page" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>-->
</UserControl>