<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow2" Height="720" Width="1170">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="R/Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Vertical">
                <TextBox Text="test1" Width="100" Height="100" Style="{StaticResource CircularTextBoxStyle}"/>
                <TextBox Text="test2" Width="100" Height="100" Style="{StaticResource CircularTextBoxStyle}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ListBox x:Name="outputListBox" />
                <TextBox x:Name="inputTextBox" KeyDown="TextBoxKeyDown" Width="420" Height="30"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
